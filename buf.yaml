version: v2

modules:
  - path: .
    name: buf.build/pandora-suite/proto

deps:
  - buf.build/envoyproxy/protoc-gen-validate

lint:
  use:
    - STANDARD
  except:
    - RPC_RESPONSE_STANDARD_NAME
    - RPC_REQUEST_RESPONSE_UNIQUE
